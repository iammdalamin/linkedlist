def append_at_begining(self, data):
    node = Node(data, self.head.next)
    self.head.next = node

def append_at_begining(self, data):
    node = Node(data, self.head.next)
    self.head.next = node

def appent_at_begining(self,data):
    node =Node(data, self.head.next)
    self.head.next = node

def append_at_begining(self, data):
    node = Node(data, self.head.next)
    self.head.next = node

def append_at_begining(self, data):
    node = Node(data, self.head.next)
    self.head.next = node